import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    titleContainer: {
        alignItems: 'center',
        marginBottom: 20, 
        marginTop: '25%'
      },
      titleText: {
        fontSize: 35,
        color: 'white',
        fontFamily: 'Itim-Regular'
      },
})